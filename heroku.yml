setup:
  addons:
    - plan: heroku-postgresql:hobby-dev
    - plan: cloudamqp:lemur
    - plan: heroku-redis:mini
    - plan: papertrail

build:
  docker:
    web: Dockerfile.web

run:
  web: sh -c "dotnet ApiGateway.dll --urls http://0.0.0.0:$PORT"
