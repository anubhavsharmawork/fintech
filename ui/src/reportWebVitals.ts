// reportWebVitals is intentionally a no-op in CI/build environments where
// the optional `web-vitals` package may not be installed. This keeps the
// production build deterministic while allowing local devs to opt-in.
export default function reportWebVitals(_onReport?: (metric: any) => void): void {
  // no-op
}
